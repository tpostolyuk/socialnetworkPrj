const initState = {
  names: [
    {id: 1, name: 'Vova'},
    {id: 3, name: 'Dmitriy'},
    {id: 4, name: 'Sofiia'},
    {id: 5, name: 'Pavlo'}
  ]
}

const dialogNameReducer = (state = initState, action) => {
  return state;
}

export default dialogNameReducer;
